import { styled } from 'styled-components';

export const Btn = styled('button')(() => {
  return {
    backgroundColor: '#353fd0',
    color: '#bdc4e0',
    border: 'none',
    padding: '7px',
    borderRadius: '5px',
    fontSize: '20px',
  };
});
